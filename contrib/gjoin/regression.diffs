diff -U3 /home/user/work/postgres/contrib/gjoin/expected/basic.out /home/user/work/postgres/contrib/gjoin/results/basic.out
--- /home/user/work/postgres/contrib/gjoin/expected/basic.out	2026-05-10 01:28:03.182964376 +0200
+++ /home/user/work/postgres/contrib/gjoin/results/basic.out	2026-05-10 02:02:28.153911668 +0200
@@ -0,0 +1,130 @@
+CREATE TABLE t1 (a INT, b INT);
+CREATE TABLE t2 (c INT, d INT);
+INSERT INTO t1 SELECT i, 1000 * random() FROM generate_series(1,10000) s(i);
+INSERT INTO t2 SELECT i, 1000 * random() FROM generate_series(1,10000) s(i);
+VACUUM ANALYZE t1;
+VACUUM ANALYZE t2;
+LOAD 'gjoin';
+SET gjoin.enabled = true;
+EXPLAIN (VERBOSE, COSTS OFF, BUFFERS OFF) SELECT * FROM t1 JOIN t2 ON (t1.A = t2.C);
+            QUERY PLAN            
+----------------------------------
+ Custom Join (GJoin) Join
+   Output: t1.a, t1.b, t2.c, t2.d
+   Join Clause: (t1.a = t2.c)
+   ->  Seq Scan on public.t1
+         Output: t1.a, t1.b
+   ->  Seq Scan on public.t2
+         Output: t2.c, t2.d
+(7 rows)
+
+EXPLAIN (ANALYZE, VERBOSE, COSTS OFF, TIMING OFF, BUFFERS OFF, SUMMARY OFF) SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c);
+                         QUERY PLAN                         
+------------------------------------------------------------
+ Custom Join (GJoin) Join (actual rows=10000.00 loops=1)
+   Output: t1.a, t1.b, t2.c, t2.d
+   Join Clause: (t1.a = t2.c)
+   ->  Seq Scan on public.t1 (actual rows=10000.00 loops=1)
+         Output: t1.a, t1.b
+   ->  Seq Scan on public.t2 (actual rows=10000.00 loops=1)
+         Output: t2.c, t2.d
+(7 rows)
+
+EXPLAIN (VERBOSE, ANALYZE, COSTS OFF, TIMING OFF, BUFFERS OFF, SUMMARY OFF) SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c) LIMIT 100;
+                            QUERY PLAN                            
+------------------------------------------------------------------
+ Limit (actual rows=100.00 loops=1)
+   Output: t1.a, t1.b, t2.c, t2.d
+   ->  Custom Join (GJoin) Join (actual rows=100.00 loops=1)
+         Output: t1.a, t1.b, t2.c, t2.d
+         Join Clause: (t1.a = t2.c)
+         ->  Seq Scan on public.t1 (actual rows=10000.00 loops=1)
+               Output: t1.a, t1.b
+         ->  Seq Scan on public.t2 (actual rows=10000.00 loops=1)
+               Output: t2.c, t2.d
+(9 rows)
+
+EXPLAIN (VERBOSE, ANALYZE, COSTS OFF, TIMING OFF, BUFFERS OFF, SUMMARY OFF) SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c) LIMIT 200;
+                            QUERY PLAN                            
+------------------------------------------------------------------
+ Limit (actual rows=200.00 loops=1)
+   Output: t1.a, t1.b, t2.c, t2.d
+   ->  Custom Join (GJoin) Join (actual rows=200.00 loops=1)
+         Output: t1.a, t1.b, t2.c, t2.d
+         Join Clause: (t1.a = t2.c)
+         ->  Seq Scan on public.t1 (actual rows=10000.00 loops=1)
+               Output: t1.a, t1.b
+         ->  Seq Scan on public.t2 (actual rows=10000.00 loops=1)
+               Output: t2.c, t2.d
+(9 rows)
+
+EXPLAIN (VERBOSE, ANALYZE, COSTS OFF, TIMING OFF, BUFFERS OFF, SUMMARY OFF) SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c) LIMIT 500;
+                            QUERY PLAN                            
+------------------------------------------------------------------
+ Limit (actual rows=500.00 loops=1)
+   Output: t1.a, t1.b, t2.c, t2.d
+   ->  Custom Join (GJoin) Join (actual rows=500.00 loops=1)
+         Output: t1.a, t1.b, t2.c, t2.d
+         Join Clause: (t1.a = t2.c)
+         ->  Seq Scan on public.t1 (actual rows=10000.00 loops=1)
+               Output: t1.a, t1.b
+         ->  Seq Scan on public.t2 (actual rows=10000.00 loops=1)
+               Output: t2.c, t2.d
+(9 rows)
+
+EXPLAIN (VERBOSE, ANALYZE, COSTS OFF, TIMING OFF, BUFFERS OFF, SUMMARY OFF) SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c) LIMIT 1000;
+                            QUERY PLAN                            
+------------------------------------------------------------------
+ Limit (actual rows=1000.00 loops=1)
+   Output: t1.a, t1.b, t2.c, t2.d
+   ->  Custom Join (GJoin) Join (actual rows=1000.00 loops=1)
+         Output: t1.a, t1.b, t2.c, t2.d
+         Join Clause: (t1.a = t2.c)
+         ->  Seq Scan on public.t1 (actual rows=10000.00 loops=1)
+               Output: t1.a, t1.b
+         ->  Seq Scan on public.t2 (actual rows=10000.00 loops=1)
+               Output: t2.c, t2.d
+(9 rows)
+
+EXPLAIN (VERBOSE, ANALYZE, COSTS OFF, TIMING OFF, BUFFERS OFF, SUMMARY OFF) SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c) LIMIT 5000;
+                            QUERY PLAN                            
+------------------------------------------------------------------
+ Limit (actual rows=5000.00 loops=1)
+   Output: t1.a, t1.b, t2.c, t2.d
+   ->  Custom Join (GJoin) Join (actual rows=5000.00 loops=1)
+         Output: t1.a, t1.b, t2.c, t2.d
+         Join Clause: (t1.a = t2.c)
+         ->  Seq Scan on public.t1 (actual rows=10000.00 loops=1)
+               Output: t1.a, t1.b
+         ->  Seq Scan on public.t2 (actual rows=10000.00 loops=1)
+               Output: t2.c, t2.d
+(9 rows)
+
+EXPLAIN (VERBOSE, ANALYZE, COSTS OFF, TIMING OFF, BUFFERS OFF, SUMMARY OFF) SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c) LIMIT 10000;
+                            QUERY PLAN                            
+------------------------------------------------------------------
+ Limit (actual rows=10000.00 loops=1)
+   Output: t1.a, t1.b, t2.c, t2.d
+   ->  Custom Join (GJoin) Join (actual rows=10000.00 loops=1)
+         Output: t1.a, t1.b, t2.c, t2.d
+         Join Clause: (t1.a = t2.c)
+         ->  Seq Scan on public.t1 (actual rows=10000.00 loops=1)
+               Output: t1.a, t1.b
+         ->  Seq Scan on public.t2 (actual rows=10000.00 loops=1)
+               Output: t2.c, t2.d
+(9 rows)
+
+EXPLAIN (VERBOSE, ANALYZE, COSTS OFF, TIMING OFF, BUFFERS OFF, SUMMARY OFF) SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c);
+                         QUERY PLAN                         
+------------------------------------------------------------
+ Custom Join (GJoin) Join (actual rows=10000.00 loops=1)
+   Output: t1.a, t1.b, t2.c, t2.d
+   Join Clause: (t1.a = t2.c)
+   ->  Seq Scan on public.t1 (actual rows=10000.00 loops=1)
+         Output: t1.a, t1.b
+   ->  Seq Scan on public.t2 (actual rows=10000.00 loops=1)
+         Output: t2.c, t2.d
+(7 rows)
+
+DROP TABLE t1;
+DROP TABLE t2;
diff -U3 /home/user/work/postgres/contrib/gjoin/expected/compare.out /home/user/work/postgres/contrib/gjoin/results/compare.out
--- /home/user/work/postgres/contrib/gjoin/expected/compare.out	2026-05-10 01:46:24.282833235 +0200
+++ /home/user/work/postgres/contrib/gjoin/results/compare.out	2026-05-10 02:02:29.763911799 +0200
@@ -0,0 +1,99 @@
+LOAD 'gjoin';
+SET gjoin.enabled = false;
+CREATE TABLE t1 (a INT, b INT);
+CREATE TABLE t2 (c INT, d INT);
+CREATE OR REPLACE FUNCTION compare_joins(sql TEXT) RETURNS bool AS $$
+DECLARE
+    v_custom_rows INT;
+    v_rows        INT;
+BEGIN
+
+    PERFORM set_config('gjoin.enabled', '1', false);
+    EXECUTE 'SELECT COUNT(*) FROM (' || sql || ')' INTO v_custom_rows;
+
+    PERFORM set_config('gjoin.enabled', '0', false);
+    EXECUTE 'SELECT COUNT(*) FROM (' || sql || ')' INTO v_rows;
+
+    IF v_rows != v_custom_rows THEN
+        RAISE WARNING '% %', v_rows, v_custom_rows;
+    END IF;
+
+    RETURN (v_rows = v_custom_rows);
+END;
+$$ LANGUAGE plpgsql;
+INSERT INTO t1 SELECT i, 1000 * random() FROM generate_series(1,10000) s(i);
+INSERT INTO t2 SELECT i, 1000 * random() FROM generate_series(1,10000) s(i);
+VACUUM ANALYZE t1;
+VACUUM ANALYZE t2;
+SELECT compare_joins('SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c)');
+ compare_joins 
+---------------
+ t
+(1 row)
+
+TRUNCATE t1;
+TRUNCATE t2;
+INSERT INTO t1 SELECT i, 1000 * random() FROM generate_series(1,10000) s(i) ORDER BY random();
+INSERT INTO t2 SELECT i, 1000 * random() FROM generate_series(1,10000) s(i) ORDER BY random();
+VACUUM ANALYZE t1;
+VACUUM ANALYZE t2;
+SELECT compare_joins('SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c)');
+ compare_joins 
+---------------
+ t
+(1 row)
+
+TRUNCATE t1;
+TRUNCATE t2;
+INSERT INTO t1 SELECT mod(i,1000), 1000 * random() FROM generate_series(1,10000) s(i);
+INSERT INTO t2 SELECT mod(i,1000), 1000 * random() FROM generate_series(1,10000) s(i);
+VACUUM ANALYZE t1;
+VACUUM ANALYZE t2;
+SELECT compare_joins('SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c)');
+WARNING:  100000 98736
+ compare_joins 
+---------------
+ f
+(1 row)
+
+TRUNCATE t1;
+TRUNCATE t2;
+INSERT INTO t1 SELECT mod(i,1000), 1000 * random() FROM generate_series(1,10000) s(i) ORDER BY random();
+INSERT INTO t2 SELECT mod(i,1000), 1000 * random() FROM generate_series(1,10000) s(i) ORDER BY random();
+VACUUM ANALYZE t1;
+VACUUM ANALYZE t2;
+SELECT compare_joins('SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c)');
+WARNING:  100000 98736
+ compare_joins 
+---------------
+ f
+(1 row)
+
+TRUNCATE t1;
+TRUNCATE t2;
+INSERT INTO t1 SELECT mod(i,100), 1000 * random() FROM generate_series(1,10000) s(i);
+INSERT INTO t2 SELECT mod(i,100), 1000 * random() FROM generate_series(1,10000) s(i);
+VACUUM ANALYZE t1;
+VACUUM ANALYZE t2;
+SELECT compare_joins('SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c)');
+WARNING:  1000000 869120
+ compare_joins 
+---------------
+ f
+(1 row)
+
+TRUNCATE t1;
+TRUNCATE t2;
+INSERT INTO t1 SELECT mod(i,100), 1000 * random() FROM generate_series(1,10000) s(i) ORDER BY random();
+INSERT INTO t2 SELECT mod(i,100), 1000 * random() FROM generate_series(1,10000) s(i) ORDER BY random();
+VACUUM ANALYZE t1;
+VACUUM ANALYZE t2;
+SELECT compare_joins('SELECT * FROM t1 JOIN t2 ON (t1.a = t2.c)');
+WARNING:  1000000 869120
+ compare_joins 
+---------------
+ f
+(1 row)
+
+DROP TABLE t1;
+DROP TABLE t2;
